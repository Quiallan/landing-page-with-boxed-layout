/* General styles */
body {
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
    background-image: url('https://cdn.pixabay.com/photo/2016/11/20/10/44/bike-1843937_1280.jpg');
    background-size: cover;
    background-attachment: fixed;
}

/* Container for all elements */
.outer-container {
    display: flex;
    justify-content: center;
    align-items: stretch;  /* Makes sure all children stretch to the same height */
    margin-top: 20px;
    min-height: calc(100vh - 285px);  /* Account for iframe height */
}

/* Style for ad banners */
.ad-banner {
    display: flex;  /* Added to center the text vertically */
    align-items: center;  /* Added to center the text vertically */
    justify-content: center;  /* Added to center the text horizontally */
    flex-direction: column;  /* Added to stack the text and image */
    position: relative;
    width: 200px;
    flex-grow: 1;  /* Makes sure it takes up the available space */
    background-color: grey;
    color: white;
    font-size: 24px;
}

/* Positioning for the banner images */
.banner-image.left, .banner-image.right {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.5;
}

/* Style for the main content */
.boxed-container {
    flex-grow: 3;  /* Makes sure it takes up the available space */
    max-width: 800px;
    background-color: #f2f2f2;
    padding: 20px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    margin: 0 20px;
}

.content-container {
    padding: 20px;
    text-align: center;
}

/* Style for rows */
.row {
    display: flex;
    justify-content: space-around;
    margin-bottom: 20px;
}

/* Style for app buttons */
.app-button {
    background-color: #007bff;
    color: white;
    border: none;
    padding: 15px 30px;
    font-size: 16px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

.app-button:hover {
    background-color: #0056b3;
}

/* Dark mode styling */
.dark-mode {
    background-color: #333;
    color: #fff;
}
